name: 'Build macOS ARM'
on:
  workflow_call:
    inputs:
      python_version:
        description: 'Python version to use'
        required: false
        default: '3.10'
        type: string
      chromium_version:
        description: 'Ungoogled Chromium version'
        required: false
        default: '142.0.7444.59-1.1'
        type: string
      chromedriver_version:
        description: 'ChromeDriver version'
        required: false
        default: '142.0.7444.61'
        type: string

env:
  CHROMIUM_BASE_URL: 'https://github.com/ungoogled-software/ungoogled-chromium-macos/releases/download'
  CHROMEDRIVER_BASE_URL: 'https://storage.googleapis.com/chrome-for-testing-public'
  OUTPUT_DIR: 'ungoogled_chromium'

jobs:
  build-macos-arm:
    name: Bundle Executable (ARM)
    runs-on: macos-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ inputs.python_version }}

      - name: Install pipenv
        run: |
          pip install --user pipenv
          echo "$HOME/Library/Python/${{ inputs.python_version }}/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          pipenv install --dev

      - name: Cache Ungoogled Chromium
        id: cache-chromium
        uses: actions/cache@v4
        with:
          path: ${{ env.OUTPUT_DIR }}
          key: ${{ runner.os }}-arm-chromium-${{ inputs.chromium_version }}

      - name: Download Ungoogled Chromium
        if: steps.cache-chromium.outputs.cache-hit != 'true'
        run: |
          curl -L -o ungoogled-chromium.dmg \
            ${{ env.CHROMIUM_BASE_URL }}/${{ inputs.chromium_version }}/ungoogled-chromium_${{ inputs.chromium_version }}_arm64-macos.dmg
          mkdir -p ${{ env.OUTPUT_DIR }}
          hdiutil attach ungoogled-chromium.dmg

          # Dynamically get the volume name
          VOLUME_NAME=$(ls /Volumes | grep -i "Chromium")
          ditto /Volumes/"$VOLUME_NAME"/Chromium.app ${{ env.OUTPUT_DIR }}/Chromium.app

          hdiutil detach /Volumes/"$VOLUME_NAME"

          # Clear the quarantine attribute that can interfere with execution
          xattr -dr com.apple.quarantine ${{ env.OUTPUT_DIR }}/Chromium.app || true

          ls -la ${{ env.OUTPUT_DIR }}
          rm -f ungoogled-chromium.dmg

      - name: Cache ChromeDriver 
        id: cache-chromedriver
        uses: actions/cache@v4
        with:
          path: ${{ env.OUTPUT_DIR }}/chromedriver
          key: ${{ runner.os }}-arm-chromedriver-${{ inputs.chromedriver_version }}

      - name: Download ChromeDriver
        if: steps.cache-chromedriver.outputs.cache-hit != 'true'
        run: |
          curl -L -o chromedriver.zip \
            ${{ env.CHROMEDRIVER_BASE_URL }}/${{ inputs.chromedriver_version }}/mac-arm64/chromedriver-mac-arm64.zip
          unzip chromedriver.zip
          mv chromedriver-mac-arm64/chromedriver ${{ env.OUTPUT_DIR }}/
          rm -rf chromedriver-mac-arm64 chromedriver.zip

      - name: Debug browser bundle
        run: python3 scripts/debug_browser_binaries.py --search-root "${{ env.OUTPUT_DIR }}"

      - name: Package the app using PyInstaller
        run: |
          export PYINSTALLER_OSX_CODESIGN=0
          export PYINSTALLER_OSX_SPLIT=0
          pipenv run pyinstaller main.spec

      - name: Archive build output
        run: |
          ditto -c -k --sequesterRsrc --keepParent dist/copliot_enigma_macos_arm64 dist/copliot_enigma_macos_arm64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macOS ARM Executable
          path: dist/copliot_enigma_macos_arm64.zip
